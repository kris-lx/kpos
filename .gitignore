# Environment
.env
.env.*
!.env.example

# Confidential files
KPOS-AGENT-TASKS.md
promp.md
rbac-1M-10M-full-isolation-fixed.md
APIS/prompt.md
APIS/RBAC_AUDIT_REPORT.md
DEPLOYMENT.md
build-prod.ps1
build-prod.sh
nginx.conf

# Temporary files
temp_url.txt

# Docker runtime secrets only (init scripts are safe to track)
# docker-compose.yml, Dockerfiles, and .dockerignore are tracked — no secrets inside
